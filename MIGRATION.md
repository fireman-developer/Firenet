# مهاجرت رابط کاربری Firenet به Jetpack Compose

این سند وضعیت مهاجرت، تصمیم‌های معماری و کارهای باقی‌مانده را ثبت می‌کند.

---

## چرا Compose و نه Flutter

درخواست اولیه، مهاجرت به Flutter بود. پس از بررسی سورس، مسیر Jetpack Compose انتخاب شد.
دلیل فنی، نه سلیقه‌ای:

هسته‌ی این برنامه قابل مهاجرت به Flutter نیست. این اجزا الزاماً بومی می‌مانند:

| جزء | چرا بومی می‌ماند |
|---|---|
| `V2RayVpnService` | پیاده‌سازی `android.net.VpnService`؛ Flutter دسترسی به آن ندارد |
| `AndroidLibXrayLite` | کتابخانه‌ی Go که با gomobile به AAR تبدیل شده |
| `tun2socks` / `hev-socks5-tunnel` | کتابخانه‌های بومی `.so` |
| `hysteria2` | همان وضعیت |
| `QSTileService`، `WidgetProvider`، `BootReceiver` | نقاط ورود سیستم‌عامل |
| پروکسی هر برنامه، Kill Switch | در سطح رابط شبکه‌ی VPN اعمال می‌شوند |

یعنی در سناریوی Flutter، دقیقاً همین کد Kotlin باقی می‌ماند و فقط یک لایه‌ی
`MethodChannel`/`EventChannel` بین آن و UI اضافه می‌شود. هزینه‌ی آن:

- حدود ۱۰ تا ۱۵ مگابایت به حجم هر ABI اضافه می‌شود،
- زمان راه‌اندازی سرد بیشتر می‌شود،
- دو زنجیره‌ی بیلد (Gradle + Flutter) باید نگه‌داری شود،
- و هر تغییر وضعیت تونل باید از میان یک پل سریال‌سازی عبور کند.

سود آن — «ارتقای راحت‌تر UI» — با Compose بدون هیچ‌کدام از این هزینه‌ها به دست می‌آید.
اگر در آینده نسخه‌ی iOS یا دسکتاپ در برنامه باشد، Flutter دوباره ارزش بررسی دارد؛
آن‌وقت هم هسته‌ی iOS باید از صفر با `NetworkExtension` نوشته شود.

**هسته‌ی VPN در این مهاجرت دست نخورده است.** هیچ فایلی در
`service/`، `handler/`، `fmt/`، `dto/`، `plugin/` یا `net/` تغییر نکرده.

---

## طراحی جدید

سبک: **شیشه‌ی مایع (liquid glass)** روی پس‌زمینه‌ی تیره‌ی عمیق.

هر سطح شیشه‌ای از چهار لایه ساخته می‌شود: پرکننده‌ی گرادیانی نیمه‌شفاف، بازتاب
تخصصی لبه‌ی بالا، حاشیه‌ی گرادیانی، و هاله‌ی رنگی وابسته به وضعیت.
عمداً از `Modifier.blur` استفاده نشده چون از API 31 به بالا کار می‌کند و
`minSdk` پروژه ۲۳ است؛ به جای آن پس‌زمینه با گرادیان‌های شعاعی نرم ساخته شده که
روی همه‌ی نسخه‌ها یکسان و بدون هزینه‌ی GPU رندر می‌شود.

### چهار حالت بصری

کل رابط کاربری از یک enum رنگ می‌گیرد (`ConnectionTone`):

| حالت | رنگ | کره |
|---|---|---|
| `Idle` | نارنجی برند | کوچک، گوشه‌ی چپ، چرخش بسیار آرام |
| `Connecting` | نارنجی روشن | مرکز، چرخش سریع |
| `Connected` | سبز-فیروزه‌ای | مرکز، ایستاده روی کشور سرور |
| `Blocked` | قرمز | مرکز، بنر هشدار Kill Switch |

### چیدمان صفحه‌ی اصلی

```
┌─────────────────────────────┐
│ [☰]                  [کاربر] │  ← منوی همبرگری بالا سمت راست
│                             │
│  وضعیت اتصال (تیتر درشت)     │
│  توضیح یک‌خطی                │
│                             │
│         ╭───────╮           │
│         │ کره‌ی  │           │  ← وسط صفحه؛ در حالت قطع
│         │ زمین  │           │     کوچک می‌شود و به چپ می‌رود
│         ╰───────╯           │
│                             │
│ ┌─────────────────────────┐ │
│ │ دریافت │ ارسال │ تأخیر  │ │  ← بالای دکمه‌ها
│ └─────────────────────────┘ │
│ ┌─────────┐ ┌─────────────┐ │
│ │ کشویی   │ │ انتخاب سرور │ │  ← اتصال راست، لوکیشن چپِ آن
│ │ اتصال   │ │             │ │
│ └─────────┘ └─────────────┘ │
└─────────────────────────────┘
```

در چیدمان راست‌به‌چپ فارسی، اولین فرزند `Row` سمت راست می‌نشیند؛ پس کنترل اتصال
سمت راست و کادر انتخاب سرور در سمت چپ آن قرار می‌گیرد. در حالت انگلیسی به‌طور
خودکار آینه می‌شود.

### کره‌ی زمین

پیاده‌سازی در `ui/compose/globe/`:

- `WorldMask.kt` — نقشه‌ی خشکی‌ها. داده از Natural Earth 1:50m، نمونه‌برداری‌شده
  روی حلقه‌های عرض جغرافیایی با گام ۲٫۸ درجه. به جای ذخیره‌ی مختصات، فقط یک بیت
  به ازای هر نقطه‌ی نامزد ذخیره شده و خود نقاط در زمان اجرا بازتولید می‌شوند:
  **۱۵۰۴ نقطه‌ی خشکی در ۶۵۸ بایت**.
- `WorldGlobe.kt` — رسم با `Canvas` و تصویربرداری متعامد (orthographic).
  عمق هر نقطه هم اندازه و هم روشنایی آن را کنترل می‌کند؛ بدون هیچ موتور سه‌بعدی،
  انحنای کره حس می‌شود. شبکه‌ی نصف‌النهار/مدار، نور لبه و سایه‌ی نیم‌کره‌ی پشتی
  هم رسم می‌شود.
- `CountryCoordinates.kt` — مختصات ۱۱۵ کشور. کلیدها همان کدهایی هستند که
  `CountryUtils` از نام کانفیگ استخراج می‌کند، پس پرچم و نشانگر روی کره هرگز به
  دو کشور مختلف اشاره نمی‌کنند.

هنگام اتصال، کره رو به جلو می‌چرخد تا طول جغرافیایی سرور دقیقاً وسط بایستد، سپس
با نوسان ±۷ درجه زنده می‌ماند.

---

## نقشه‌ی فایل‌ها

```
ui/compose/
├── theme/
│   ├── Color.kt          پالت + ConnectionTone
│   ├── Type.kt           تایپوگرافی (ارتفاع خط بازتر برای فارسی)
│   └── Theme.kt          FirenetTheme
├── components/
│   ├── Glass.kt          Modifier.liquidGlass، GlassPanel، GlassChip، GlassIconButton
│   ├── Backdrop.kt       AuroraBackdrop
│   ├── SlideToConnect.kt کنترل کشویی اتصال
│   ├── Lifecycle.kt      rememberIsResumed — مهار انیمیشن‌های بی‌پایان
│   └── HomeControls.kt   MetricsBar، LocationSelector
├── globe/
│   ├── WorldMask.kt
│   ├── WorldGlobe.kt
│   └── CountryCoordinates.kt
├── home/
│   ├── HomeUiState.kt    وضعیت تغییرناپذیر صفحه
│   ├── HomeViewModel.kt  تنها نقطه‌ی تماس با هسته
│   ├── HomeScreen.kt     چیدمان صفحه
│   ├── SideMenu.kt       منوی کناری
│   ├── ServerPicker.kt   فهرست انتخاب سرور
│   └── HomeRoute.kt      پوسته: کشو، شیت، گفت‌وگوها
├── login/
│   └── LoginScreen.kt
└── loading/
    └── LoadingScreen.kt   صفحه‌ی راه‌اندازی
```

اجزای غیرِ UI که در فاز دوم اضافه شدند:

```
handler/
├── SpeedMonitor.kt        تنها مالک شمارنده‌های ترافیک هسته
├── BestServerFinder.kt    انتخاب خودکار بهترین لوکیشن (تأخیر + پهنای باند)
└── UiMemoryGovernor.kt    آزادسازی رابط کاربری پس از ۴۰ ثانیه پس‌زمینه
```

`MainActivity` از ۶۵۰ خط به ۲۹۰ خط رسید و حالا فقط سه کار می‌کند: نگه‌داشتن سطح
Compose، گرفتن مجوزهایی که فقط یک اکتیویتی می‌تواند بگیرد (VPN و نوتیفیکیشن)، و
جابه‌جایی بین صفحات.

---

## حذف‌شده‌ها

فایل‌های زیر پس از مهاجرت بی‌استفاده شدند و حذف شدند:

- `ui/MainRecyclerAdapter.kt`، `ui/ServerSelectionAdapter.kt`، `ui/FragmentAdapter.kt`
- `ui/widget/GlassBlurView.kt`
- `viewmodel/MainViewModel.kt`
- `res/layout/`: `activity_main`, `activity_login`, `layout_server_bottom_sheet`,
  `item_server_selection`, `item_recycler_main`, `item_recycler_footer`, `nav_header`
- `res/menu/`: `menu_main`, `menu_drawer`

---

## وضعیت صفحات

| صفحه | وضعیت |
|---|---|
| صفحه‌ی اصلی | ✅ Compose |
| منوی کناری | ✅ Compose |
| انتخاب سرور | ✅ Compose |
| ورود | ✅ Compose |
| تنظیمات | ⏳ View کلاسیک، تم تیره گرفت |
| مسیریابی + ویرایش قانون | ⏳ View کلاسیک، تم تیره گرفت |
| پروکسی هر برنامه | ⏳ View کلاسیک، تم تیره گرفت |
| دارایی‌ها | ⏳ View کلاسیک، تم تیره گرفت |
| لاگ | ⏳ View کلاسیک، تم تیره گرفت |
| درباره / بررسی به‌روزرسانی | ⏳ View کلاسیک، تم تیره گرفت |

صفحات باقی‌مانده تم `AppTheme.Firenet` گرفتند تا رفتن از صفحه‌ی تیره‌ی اصلی به یک
صفحه‌ی روشن، حس شکستگی ندهد. مهاجرت کامل آن‌ها فاز بعدی است.

**ترتیب پیشنهادی فاز بعد:** پروکسی هر برنامه ← لاگ ← دارایی‌ها ← مسیریابی ←
تنظیمات. تنظیمات را آخر بگذارید؛ روی `PreferenceFragmentCompat` با حدود ۵۰ کلید
ساخته شده و بازنویسی آن بیشترین کار را دارد.

---

## فاز دوم — سرعت واقعی، انتخاب خودکار، و مدیریت حافظه

### ۱. سرعت آپلود/دانلود، این بار واقعی

عدد قبلی از `TrafficStats.getUidRxBytes` می‌آمد و درست نبود:

- در حالت VPN هر بسته **دو بار** شمرده می‌شود؛ یک بار موقع خواندن از رابط `tun`
  و یک بار موقع نوشتن روی سوکت بیرونی.
- ترافیک خودِ برنامه (وضعیت حساب، به‌روزرسانی، پوش) هم داخلش بود.

حالا عدد از شمارنده‌های خروجی خود هسته می‌آید. چون هسته در فرایند
`:RunSoLibV2RayDaemon` است، یک کانال پیام اضافه شد:

```
UI ──MSG_SPEED_SUBSCRIBE──▶ V2RayServiceManager ──▶ SpeedMonitor
UI ◀──MSG_SPEED_TICK───────  (هر ۱ ثانیه، Pair<up,down> بایت بر ثانیه)
```

`handler/SpeedMonitor.kt` تنها مالک شمارنده‌هاست. دلیلش مهم است:
`queryStats` در Xray **خواندن-و-صفر-کردن** است. اگر نوتیفیکیشن و صفحه‌ی اصلی
جداگانه بخوانند، بایت‌های همدیگر را می‌دزدند و هر دو عدد غلط می‌شود. پس یک
حلقه‌ی نمونه‌برداری وجود دارد و نتیجه بین شنونده‌ها پخش می‌شود.

اشتراک صفحه به `onStart`/`onStop` گره خورده؛ در پس‌زمینه نمونه‌برداری قطع
می‌شود و نوتیفیکیشن با یک‌سوم نرخ کارش را می‌کند.

`stats`/`policy` دیگر هرگز از کانفیگ حذف نمی‌شوند (قبلاً با خاموش بودن
`PREF_SPEED_ENABLED` حذف می‌شدند و شمارنده‌ها صفر برمی‌گرداندند). آن تنظیم حالا
فقط نمایش سرعت در نوتیفیکیشن را کنترل می‌کند.

### ۲. بهترین لوکیشن (خودکار)

`handler/BestServerFinder.kt`، اجرا در `V2RayTestService`. دو مرحله:

| مرحله | چه چیزی | روی چند سرور | چطور |
|---|---|---|---|
| ۱ | تأخیر | همه | `measureOutboundDelay`، موازی با ۶ مجوز هم‌زمان |
| ۲ | پهنای باند واقعی | ۴ نامزد برتر | یک نمونه‌ی هسته با ورودی HTTP روی پورت آزاد، دانلود واقعی، شمارش بایت بر ثانیه |

مرحله‌ی دوم عمداً **ترتیبی** است؛ دو دانلود هم‌زمان پهنای باند همدیگر را
می‌خورند و هر دو عدد را بی‌اعتبار می‌کنند. زمان تا اولین بایت از محاسبه بیرون
گذاشته می‌شود، وگرنه سرورهای دور دو بار جریمه می‌شدند.

امتیاز: `sqrt(Mbps) × 100 ÷ (1 + تأخیر/۲۵۰)`. ریشه‌ی دوم عمدی است — تفاوت ۸ و
۱۰ مگابیت نباید سروری با تأخیر دو برابر را برنده کند.

اگر تونل بالا باشد، مرحله‌ی دوم اجرا نمی‌شود (ترافیک تست از داخل تونل فعلی عبور
می‌کرد) و رتبه‌بندی به تأخیر برمی‌گردد. نامزدهای Hysteria2 که فرایند کمکی جدا
لازم دارند، میانه‌ی پهنای باند بقیه را می‌گیرند تا نه حذف شوند و نه ناحق ببرند.

حالت **ماندگار** است: ردیف «⚡ بهترین لوکیشن (خودکار)» بالای فهرست سرورها
می‌نشیند و تا وقتی کاربر دستی سروری انتخاب نکرده، **هر بار اتصال** دوباره
سنجش انجام می‌شود. انتخاب دستی، حالت خودکار را خاموش می‌کند.

سقف ۴۵ ثانیه دارد؛ در صورت عبور، با کم‌تأخیرترین سرور شناخته‌شده جلو می‌رود تا
کاربر روی «در حال اتصال» گیر نکند.

### ۳. صفحه‌ی راه‌اندازی و آزادسازی حافظه

`ui/compose/loading/LoadingScreen.kt` + `handler/UiMemoryGovernor.kt`.

هنگام ورود، کارهای سنگین (`initRoutingRulesets` که از assets می‌خواند، ساخت
کانال نوتیفیکیشن) از نخ اصلی به پس‌زمینه رفتند و صفحه‌ی راه‌اندازی منتظرشان
می‌ماند. کف زمان نمایش ۱٫۵ ثانیه (شروع سرد) یا ۰٫۹ ثانیه (شروع گرم)، سقف ۵ ثانیه.

> در فرایند `:RunSoLibV2RayDaemon` این کارها **هم‌زمان** انجام می‌شوند، نه در
> پس‌زمینه. آنجا صفحه‌ای وجود ندارد که منتظر بماند و ممکن است بلافاصله ساخت
> کانفیگ شروع شود؛ قواعد مسیریابی نباید نیمه‌آماده خوانده شوند.

`UiMemoryGovernor` اکتیویتی‌ها را می‌شمارد (نه یک پرچم ساده — موقع رفتن از صفحه‌ی
اصلی به تنظیمات، `onStart` صفحه‌ی جدید پیش از `onStop` قبلی می‌آید). با صفر شدن
شمارنده، ۴۰ ثانیه تایمر می‌گذارد. اگر کاربر زودتر برگردد، تایمر لغو و صفحه
دقیقاً همان‌جاست که رهایش کرده بود. اگر نه:

- همه‌ی اکتیویتی‌ها `finish()` می‌شوند → درخت Compose، ViewModel‌ها و بیت‌مپ‌ها آزاد،
- کش درون-حافظه‌ای MMKV خالی می‌شود،
- فرایند به یک نامزد ارزان برای بازیابی سیستم‌عامل تبدیل می‌شود.

**سرویس تونل دست نمی‌خورد.** در فرایند دیگری اجرا می‌شود و اصلاً متوجه این
اتفاق نمی‌شود؛ اتصال قطع نمی‌شود.

نکته‌ی ظریف: `TRIM_MEMORY_UI_HIDDEN` هیچ ربطی به کمبود حافظه ندارد و درست همان
لحظه‌ای می‌آید که برنامه کوچک می‌شود. اگر آنجا آزادسازی می‌کردیم، مهلت ۴۰ ثانیه
بی‌معنا می‌شد. فقط از `TRIM_MEMORY_BACKGROUND` به بالا فوراً آزاد می‌کنیم.

### ۴. مصرف پردازنده در حالت بی‌کاری

کره در هر فریم حدود ۱۵۰۰ نقطه‌ی خشکی به‌علاوه‌ی شبکه‌ی نصف‌النهارها را از نو رسم
می‌کرد و چرخشش بی‌پایان بود — یعنی مصرف دائمی، حتی وقتی صفحه دیده نمی‌شد.
`rememberInfiniteTransition` خودش با رفتن به پس‌زمینه متوقف نمی‌شود.

`components/Lifecycle.kt::rememberIsResumed()` اضافه شد و همه‌ی انیمیشن‌های
بی‌پایان به آن گره خوردند: چرخش کره، تپش نشانگر، جابه‌جایی هاله‌های پس‌زمینه و
درخشش راهنمای کشویی اتصال. تپش نشانگر علاوه بر آن فقط در حالت متصل/در حال
اتصال اجرا می‌شود و درخشش راهنما فقط وقتی کاربر واقعاً باید کاری بکند.

---

## کارهای باقی‌مانده‌ی دیگر

1. **بیلد و رفع خطاهای کامپایل.** این مهاجرت در محیطی انجام شده که به Android SDK
   دسترسی نداشت، پس کد کامپایل نشده است. اولین بیلد را با
   `./gradlew :app:assemblePlaystoreDebug` بزنید.
2. **جداسازی متن‌های فارسی از `values/strings.xml`.** چند رشته‌ی فارسی در فایل
   انگلیسی مانده (`kill_switch_active`، `nav_*`، `status_on/off`). باید به
   `values-fa/` منتقل و معادل انگلیسی‌شان در `values/` نوشته شود.
3. **فونت.** تنها فونت پروژه `montserrat_thin.ttf` است که برای رابط کاربری نازک
   است و فارسی را هم پوشش نمی‌دهد؛ فعلاً از فونت سیستم استفاده می‌شود. اگر فونت
   اختصاصی می‌خواهید، یک خانواده‌ی کامل (مثلاً Vazirmatn) با وزن‌های ۴۰۰/۵۰۰/۷۰۰
   اضافه کنید و در `Type.kt` جایگزین `FontFamily.Default` شود.
4. **`R8` غیرفعال است** (`isMinifyEnabled = false`). با اضافه شدن Compose، فعال
   کردن آن حجم APK را محسوس کم می‌کند. قواعد نگه‌داری برای gomobile و MMKV لازم است.
